import { Layout } from "components/Layout";
import { FC } from "react";
import { Settings } from "components/sсreens/Settings/Settings";

const SettingsPage: FC = () => {
    return (
        <Layout>
            <Settings />
        </Layout>
    );
};

export default SettingsPage;
