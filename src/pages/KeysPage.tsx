import { Layout } from "components/Layout";
import { FC } from "react";
import { Keys } from "components/sсreens/Keys/Keys";

const KeysPage: FC = () => {
    return (
        <Layout>
            <Keys />
        </Layout>
    );
};

export default KeysPage;
